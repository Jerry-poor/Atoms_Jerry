# Backend
ENV=dev
TEST_MODE=false
# Non-default ports to avoid conflicts with other local services.
WEB_PORT=13000
API_PORT=18000
ATOMS_PG_PORT=15432

WEB_APP_URL=http://localhost:13000
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:15432/atoms_jerry

SESSION_COOKIE_NAME=atoms_session
SESSION_MAX_AGE_SECONDS=604800
OAUTH_SESSION_SECRET=change-this-to-a-long-random-string

# OAuth (required in non-TEST_MODE)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:18000

# Optional public links used by the marketing UI
# (Defaults are set in code; override here if desired.)
NEXT_PUBLIC_DOCS_URL=/docs
NEXT_PUBLIC_GITHUB_URL=https://github.com
NEXT_PUBLIC_COMMUNITY_URL=https://atoms.dev
